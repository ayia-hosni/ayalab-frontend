/**
 * Base URL of the Spring Boot API. The Angular dev server proxies /api to
 * http://localhost:8080 (see proxy.conf.json), so a relative path works in dev.
 * For a deployed build, set this to the absolute API origin.
 */
export const API_BASE = '/api';
