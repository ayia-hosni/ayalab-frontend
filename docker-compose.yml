# Backend runs elsewhere (http://api.ayalab.co) — nginx.conf proxies /api there directly.
# No local backend/postgres service needed in this compose file.
services:
  frontend:
    image: ayahosny/ayalab-frontend:latest
    ports:
      - "80:80"
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
